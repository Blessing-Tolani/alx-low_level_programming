#include "main.h"

/**
 * _strchr - locates a character in a string
 * @s: string
 * @c: character
 *
 * Return: a pointer to memory area
 */
char *_strchr(char *s, char c)
{
	unsigned int i;

	i = 0;
	while (i < n)
	{
		s[i] = b;
		i++:
	}
	return (s);
}
