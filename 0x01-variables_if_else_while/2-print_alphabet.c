#include <stdio.h>
/**
 * main - prints alphabet for insomnia patients
 * Return: Always 0
 */
int main(void)
{
	char ltr;

	for (ltr = "a"; ltr <= "z"; ltr++)
	putchar(ltr);

	putchar('\n');

	return (0);
}
